#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int x,a,b;
    scanf("%d%d%d",&x,&a,&b);
    int sell=x*a;
    int cost=x*b;
    int pro=sell-cost;
    int D=pro-100;
    printf("%d",D);
    return 0;
}
