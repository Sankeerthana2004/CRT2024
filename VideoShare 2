#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    int a;
    scanf("%d",&a);
    if(a>=30&&a<=50)
    {
        printf("Average");
    }
    else if(a>50&&a<=60)
    {
        printf("Good");
    }
    else if(a>60&&a<=80)
    {
        printf("Excellent");
    }
    else
    {
        printf("Invalid Input");
    }
    return 0;
}
