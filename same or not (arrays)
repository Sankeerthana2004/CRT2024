#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
 #include <stdio.h>

int main() {
int a1,b1;
    scanf("%d%d",&a1,&b1);
    int a[a1],b[b1];
    for(int i=0;i<a1;i++){
        scanf("%d",&a[i]);
    }
    for(int j=0;j<b1;j++){
        scanf("%d",&b[j]);
    }
    if(a1!=b1){
        printf("Not Same");
        return 0;
    }
    
    for(int i=0;i<a1;i++){
        if(a[i]!=b[i]){
            printf("Not Same");
            return 0;
        }
    }
    printf("Same");
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    return 0;
}
